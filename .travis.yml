language: elixir
elixir: '1.5.2'
otp_release: '19.0'

install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

env:
  - MIX_ENV=test

script:
  - mix test
  - mix coveralls.travis
